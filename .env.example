# PRD-C-1000 teacher-mcp 配置（凭据不入 git，copy 为 .env 后填）
# RuoYi C 线 book-server 底座地址
RUOYI_BASE_URL=http://localhost:8090
# RuoYi 客户端 id / 租户（双头鉴权用，非密）
RUOYI_CLIENT_ID=e5cd7e4891bf95d1d19206ce24a7b32e
RUOYI_TENANT_ID=000000

# 🔴 真账号身份：正式由 MCP `login(username,password)` 工具传参注入。
# 下面两项仅作 login 不传参时的兜底 + A1 冒烟脚本用，凭据走 .env 不提交。
RUOYI_USERNAME=
RUOYI_PASSWORD=
